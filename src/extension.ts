export function activate(): void {
  // Command and view wiring is added by the review pipeline and UI tasks.
}

export function deactivate(): void {
  // VS Code has no resources to dispose at scaffold time.
}
